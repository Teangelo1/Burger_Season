<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{devour}}">
		{{#if devour}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE ITEM!</button>
</li>
