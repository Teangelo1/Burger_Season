<img src="/assets/images/flames.png" alt="logo"/>
<h1><img id="logo" src="/assets/images/images.png" alt="logo"/>Fiyah Burger<img id="logo" src="/assets/images/images.png" alt="logo"/></h1>

<h2>Burgers you devoured</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers/burger-block devoured=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2 id="you-did-not">You did not devour these, yo</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers/burger-block devoured=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Build a Burger</h2>
<form id="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured"></label><br>
    <input type="radio" name="devoured" value="1" id="devoured" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0" id="devour"> Not Devoured
  </div>

  <button type="submit"> Add Burger </button>
</form>